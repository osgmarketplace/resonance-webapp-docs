---
id: create-partner-identity
title: createPartnerIdentity
---

Create a new PartnerIdentity (give an Identity permission to a Partner)

```graphql
createPartnerIdentity(
  input: PartnerIdentityCreateInput
): PartnerIdentity

```

### Arguments

#### `input` ([`PartnerIdentityCreateInput`](/developers/shared-apis/adminidentity-api/graphql/reference/inputs/partner-identity-create-input))

### Type

#### [`PartnerIdentity`](/developers/shared-apis/adminidentity-api/graphql/reference/objects/partner-identity)

Type representing a PartnerIdentity, the permissions given to a User for a Partner
